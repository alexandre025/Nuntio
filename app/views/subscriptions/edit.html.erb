<%= simple_form_for @subscription, url: subscription_path do |f| %>
  <%= f.input :owner_id, as: :hidden %>
  <%= f.input :tower_id, as: :hidden %>
  <%= f.input :commitment, collection: Subscription::COMMITMENTS, as: :radio_buttons %>
  <%= f.input :quantity %>
  <%= f.button :submit %>
<% end %>
